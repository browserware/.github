<!-- /autoplan restore point: /Users/Aleksei_Gurianov/.gstack/projects/browserware-browserware/pr1-browser-context-substrate-autoplan-restore-20260411-022747.md -->
# Design: Browser Context Substrate

Generated by /office-hours on 2026-04-10T17:07:12Z
Branch: reorganize-docs-match-reality
Repo: browserware/browserware
Status: APPROVED
Mode: Builder

## Problem Statement

Browserware is building a modular ecosystem for intelligent browser routing across macOS, Windows, and Linux. The current repo already has the right crate split: detection, profiles, launch, rules, system integration, shared types, and CLI.

The product risk is positioning. Existing tools already handle consumer browser picking and URL routing well enough, especially on macOS. Browserware should not start by cloning Velja, Choosy, Browserosaurus, or Finicky. It should own the lower layer they do not expose cleanly: discovering every launchable browser context on a machine and launching it predictably from code.

## What Makes This Cool

The "whoa" moment is:

```bash
brw contexts --json
```

It prints every usable browser context on the machine: Chrome Stable / Work, Chrome Canary / Testing, Edge / Client A, Firefox Developer Edition / Default, Safari / default, and unknown registered handlers where metadata is partial.

Then:

```bash
brw open https://github.com --context chromium:work
```

It opens the URL in the right context without the user remembering bundle IDs, profile-directory names, executable paths, or OS-specific launch flags.

That is useful to developers, agents, scripts, tests, and later a GUI/default-browser shim. Same foundation, many surfaces.

## Constraints

- The first release must be CLI/library-first, not GUI-first.
- The crate architecture should stay modular. Libraries must remain useful standalone.
- Detection should stay discovery-first: enumerate registered system handlers first, then enrich known browsers with registry metadata.
- Browser profiles are not uniform. Chrome-family profiles, Firefox profiles, Safari, Arc/Dia spaces, and encrypted or inaccessible profile metadata all behave differently.
- Machine-readable output matters. This is a substrate for tools, not just a human menu.
- Cross-platform parity is a goal, but the first implementation can expose platform-specific capability gaps honestly.

## Premises

1. The first product should be a developer-facing CLI and library set, not a consumer default-browser GUI.
2. The core object should be a browser context target: browser app + engine/family + channel + profile + launch capability.
3. Browserware's first wow moment should be `brw contexts --json`, showing every launchable context on the machine in a stable, scriptable format.
4. The wedge user is a developer or automation-heavy builder with work/personal/client/agent browser contexts, not a normal Mac user who just wants links to open in Safari sometimes.
5. Rules matter, but they should sit on top of context discovery and launch. If discovery is wrong, rules are lipstick on a segfault.
6. The GUI/default-browser shim is still a good long-term product, but it should reuse the same substrate instead of driving the architecture.

## Search Findings

Existing tools show the consumer link-routing space is already real:

- Browserosaurus is an open-source macOS browser prompter that sets itself as the default browser and lets users pick an installed browser for clicked links. It is archived.
- Choosy supports browser prompts, rules, browser profiles for Chrome-family browsers, browser extensions, and URL-based automation.
- Velja supports browser rules, source-app matching, browser profiles, scripting via custom URL scheme, and profile-specific launching. Its docs call out hard browser-specific limits, including Arc/Dia not supporting Chrome's `--profile-directory` flag and Atlas storing profile names in an encrypted file.
- Finicky is the hacker/config-file version: JavaScript rules, URL rewriting, browser handlers, and browser extensions.
- Microsoft Edge policy docs show an enterprise variant of this problem: external links can prioritize an app-specified profile over the default external-link profile.

The search result changes the framing. Browserware should not claim browser/profile routing is untouched. The better opening is cross-platform, developer-grade context discovery and deterministic launch.

## Approaches Considered

### Approach A: Context CLI Wedge

Ship `brw contexts` and `brw open --context <selector>` as the first real product slice. It discovers browsers, enriches them with profile data where available, prints stable JSON, and launches a URL into a selected browser/profile.

Effort: M
Risk: Low/Med

Pros:
- Fastest useful thing to ship.
- Matches the existing crate layout cleanly.
- Creates a demo developers immediately understand.

Cons:
- Less visually impressive than a GUI.
- Profile support exposes browser-specific edge cases immediately.
- Rules remain basic at first.

Reuses:
- `browserware-detect`
- `browserware-profiles`
- `browserware-launch`
- `browserware-types`
- `browserware-cli`

### Approach B: Substrate First, CLI As Proof

Define a durable `BrowserContext` / `BrowserTarget` model first, then make detection, profile lookup, launch, and CLI all use it. The CLI becomes proof that the library architecture is right.

Effort: L
Risk: Med

Pros:
- Best long-term architecture.
- Makes the future GUI/default-browser shim much cleaner.
- Reduces the chance of baking CLI-only assumptions into core types.

Cons:
- Slower than a purely command-shaped implementation.
- More design pressure up front.
- Easy to over-model profile edge cases before real users hit them.

Reuses:
- All workspace crates, especially `browserware-types` as the contract layer.

### Approach C: Automation Gateway

Treat Browserware as infrastructure for agents, scripts, and dev tools. First-class JSON APIs, deterministic selectors, and commands like "open this URL in the best isolated work profile" matter more than human picker UX.

Effort: M/L
Risk: Med/High

Pros:
- Most differentiated from Velja, Choosy, Browserosaurus, and Finicky.
- Strong fit for AI/browser automation workflows.
- Could become the browser-context layer other tools build on.

Cons:
- Harder to explain to normal users.
- Needs crisp docs and examples.
- May be too narrow if the immediate goal is a broad user-facing app.

Reuses:
- Same substrate as Approach B, but biases toward machine-readable output, predictable selectors, and scripting contracts.

## Recommended Approach

Choose Approach B, with Approach A as the first milestone.

The product should be designed as a substrate, not a pile of CLI commands. But the first thing users should see is concrete:

```bash
brw contexts --json
brw open https://github.com --context chromium:work
```

That gives the architecture a spine and the product a demo. The CLI validates the core model. The core model keeps the future GUI/default-browser shim from becoming a separate product with separate logic.

## Engineering Review Decisions

- Scope split accepted during /plan-eng-review: PR1 should implement the `BrowserContext` model and `brw contexts` output only. PR2 should add `brw open <url> --context <selector>` launch behavior. This keeps context discovery usable even if profile-specific launch has browser-specific failures.
- Selector grammar accepted during /plan-eng-review: canonical selectors should be key-value, for example `family=chromium,browser=chrome,profile=work`. Compact aliases such as `chrome:work` can exist, but only as a convenience layer over the canonical form.
- Ambiguity handling accepted during /plan-eng-review: ambiguity is allowed and should be policy-driven. Initial strategies should include first match, warn/log, and error, configurable with flags. Machine-oriented paths should default toward explicit diagnostics; human convenience paths may choose first match when requested.
- Capability model accepted during /plan-eng-review: `BrowserContext` must expose capability flags in PR1, not defer them to launch. Include `discoverable`, `launchable`, `profile_launchable`, `requires_user_config`, and `limitations: Vec<String>` so contexts that can be discovered but not launched do not look equivalent to fully launchable contexts.
- Distribution scope accepted during /plan-eng-review: crates.io publishing is not in scope for PR1. Existing GitHub Release binary automation is enough for the first context-substrate implementation, but crates.io publishing must be captured as follow-up release work because the design promises `cargo install browserware-cli`.
- CLI organization accepted during /plan-eng-review: add small CLI modules for PR1, at minimum `commands/contexts.rs` and `output.rs`, so `main.rs` does not accumulate command orchestration, JSON shapes, and table rendering for every feature. Do not do a full CLI refactor in PR1 unless required by the contexts command.
- Test strategy accepted during /plan-eng-review: profile discovery must use deterministic fixtures, not whatever browser profiles exist on the CI machine. PR1 should include fixtures for Chrome-family `Local State`/profile directories, Firefox `profiles.ini`, no-profile/default-only browsers, and inaccessible or encrypted metadata cases.
- JSON stability accepted during /plan-eng-review: `brw contexts --json` is a public automation API and needs golden output tests, not just parseability checks. Public context structs also need serde round-trip tests.

## NOT in Scope for PR1

- `brw open <url> --context <selector>` launch behavior: deferred to PR2 so context enumeration can ship independently.
- Rules engine changes: deferred until discovery and context output are trustworthy.
- GUI/default-browser shim: long-term product surface, not part of the substrate PR.
- crates.io publishing workflow: existing GitHub Release binary automation remains, but `cargo install browserware-cli` publishing needs a separate release task.

## Open Questions

- What are the exact flag names for ambiguity handling, for example `--on-ambiguous first|warn|error` or shorter alternatives?
- Should `brw contexts` show ready-to-copy canonical selectors for each context in table output?
- Which profile metadata is safe to expose in JSON by default?
- How should Browserware represent contexts that are discoverable but not launchable, such as Arc/Dia profile-like spaces or browsers with encrypted profile names?
- Should the first milestone include rules at all, or only context enumeration and explicit launch?

## Success Criteria

- `brw contexts` returns a stable, documented list of browser contexts.
- `brw contexts --json` is machine-readable and suitable for scripts and agent workflows.
- `brw open <url> --context <selector>` launches into the intended browser context where the platform/browser supports it.
- Ambiguous selectors produce useful errors with suggested exact selectors.
- Unknown browsers remain visible rather than silently disappearing.
- Capability gaps are explicit: a context can be detected but marked as not profile-launchable.
- The same `BrowserContext`/`BrowserTarget` model can support a future GUI/default-browser shim.

## Distribution Plan

Browserware is a Rust workspace intended for crates.io distribution.

- Publish the library crates and `browserware-cli` to crates.io.
- Users install the CLI with `cargo install browserware-cli`.
- CI should build and test the workspace on macOS, Windows, and Linux.
- Release builds should use `--locked` to respect the committed `Cargo.lock`.
- Future GUI/default-browser app can live in a separate closed-source repo but must depend on the same open-source substrate contracts.

## Next Steps

1. PR1: Define the shared context model in `browserware-types`: `BrowserContext`, `ProfileRef`, `LaunchCapability`, `ContextSelector`, and explicit capability flags.
2. PR1: Extend `browserware-profiles` to return profile metadata with capability flags instead of pretending all browsers support the same launch behavior.
3. PR1: Add `brw contexts` with human table output and `--json`.
4. PR1: Document known browser limitations: Chrome-family `--profile-directory`, Firefox profiles, Safari limitations, Arc/Dia spaces, encrypted profile metadata.
5. PR2: Extend `browserware-launch` to accept a context target, not just an executable plus loose options.
6. PR2: Add `brw open <url> --context <selector>`, with exact-match behavior first and helpful ambiguity errors.

## Implementation Notes

PR1 should keep the CLI diff intentionally small:

- Add `commands/contexts.rs` for the `brw contexts` command.
- Add `output.rs` for reusable table/JSON/plain formatting helpers where they reduce duplication.
- Avoid a full migration of the existing `browsers` command unless the contexts command needs shared formatting.

PR1 tests should be fixture-driven:

- Add Chrome-family fixtures covering multiple profile directories and display names.
- Add Firefox fixtures covering `profiles.ini` with relative and absolute profile paths.
- Add default-only/no-profile cases for browsers such as Safari.
- Add inaccessible/encrypted/unknown metadata cases that produce capability limitations instead of panics or silent omission.
- Add CLI integration tests for `brw contexts --format json`, table output, plain output, and empty/no-context behavior.
- Add golden JSON tests for representative `brw contexts --format json` output and serde round-trip tests for public context structs.

## What I noticed about how you think

- You kept the CLI and GUI in the same product family instead of collapsing them into one thing. That is the right separation: one substrate, multiple surfaces.
- You accepted the search result changing the framing. That matters. The market already has pickers, so the product has to move down a layer where it can be more useful.
- You are building around a real developer annoyance: browser/profile context is state people carry in their heads because tools do not expose it cleanly.

## The Assignment

Before implementation, write three concrete user stories using real machine states:

1. A developer with Chrome Stable personal, Chrome Stable work, Chrome Canary testing, and Firefox Developer Edition.
2. An automation/agent workflow that needs an isolated browser profile for each project.
3. A future GUI/default-browser workflow that needs to choose a context automatically.

For each story, write the exact `brw contexts --json` shape you wish existed and the exact `brw open` command that should work. If those examples feel awkward, fix the model before writing code.

## PLAN ENG REVIEW REPORT

Generated by /plan-eng-review on 2026-04-10
Status: CLEAN

### Scope Challenge

Accepted scope reduction: PR1 implements context discovery and `brw contexts` only. PR2 implements `brw open <url> --context <selector>`.

This keeps the first implementation useful while reducing blast radius. Context enumeration and profile-specific launch are separate failure domains.

### What Already Exists

- `browserware-detect` already returns installed `Browser` values using platform-specific discovery.
- `browserware-cli` already supports `browsers`, `profiles`, `open`, `config`, `register`, and `unregister` command shells.
- `browserware-cli` already has global `--format table|json|plain`.
- `browserware-profiles`, `browserware-launch`, `browserware-rules`, and `browserware-system` are scaffolds.
- GitHub Release automation already builds `brw` binaries for Linux, macOS, and Windows, but crates.io publishing does not exist yet.

### Architecture Review

Issues found: 4
Resolved: 4

1. Split context enumeration and launch into separate PRs. Accepted.
2. Use canonical key-value selectors, for example `family=chromium,browser=chrome,profile=work`. Accepted.
3. Model ambiguity as policy: first match, warn/log, or error. Accepted.
4. Add explicit capability flags in PR1: `discoverable`, `launchable`, `profile_launchable`, `requires_user_config`, and `limitations`. Accepted.

### Data Flow

```text
PR1: brw contexts
=================

platform discovery
    |
    v
browserware-detect::detect_browsers()
    |
    v
Vec<Browser>
    |
    v
browserware-profiles::discover_profiles(browser)
    |
    +--> profiles found --------------------+
    |                                       |
    +--> no profiles/default-only ----------+
    |                                       |
    +--> metadata inaccessible/encrypted ---+
                                            |
                                            v
                                 Vec<BrowserContext>
                                            |
                                            v
                              selector + capability enrichment
                                            |
                                            v
                          brw contexts --format table|json|plain
```

### Code Quality Review

Issues found: 1
Resolved: 1

1. Avoid growing `main.rs` into a command/output God file. Accepted: add small `commands/contexts.rs` and `output.rs` modules, without a full CLI refactor.

### Test Review

Issues found: 2
Resolved: 2

1. Add deterministic fixture-driven profile tests. Accepted.
2. Add golden JSON output tests plus serde round-trip tests. Accepted.

```text
CODE PATH COVERAGE REQUIRED FOR PR1
===================================

[+] browserware-types
    |
    +-- BrowserContext
    |   +-- [REQUIRED] serialize/deserialize full context
    |   +-- [REQUIRED] context with no profile
    |   +-- [REQUIRED] context with profile
    |   +-- [REQUIRED] context with limitations
    |
    +-- ContextSelector
        +-- [REQUIRED] parse canonical key-value selector
        +-- [REQUIRED] reject malformed selector
        +-- [REQUIRED] match exact context
        +-- [REQUIRED] ambiguous match, policy=first
        +-- [REQUIRED] ambiguous match, policy=warn
        +-- [REQUIRED] ambiguous match, policy=error

[+] browserware-profiles
    |
    +-- discover_profiles(browser)
        +-- [REQUIRED] Chrome-family Local State/profile dirs fixture
        +-- [REQUIRED] Firefox profiles.ini fixture
        +-- [REQUIRED] no profile support/default-only case
        +-- [REQUIRED] inaccessible/encrypted metadata limitation

[+] browserware-cli
    |
    +-- brw contexts
        +-- [REQUIRED] table output includes selector + capability summary
        +-- [REQUIRED] json output matches golden output
        +-- [REQUIRED] plain output is script-friendly
        +-- [REQUIRED] no contexts found exits successfully with clear message
```

### Performance Review

Issues found: 0

No performance blockers for PR1. The main guardrail is avoiding repeated full browser/profile discovery inside a single `brw contexts` run. Do one discovery pass, enrich contexts, then render output.

### Failure Modes

| Failure mode | Test required | Error handling required | User-visible behavior |
|--------------|---------------|-------------------------|-----------------------|
| Browser has no profiles | yes | yes | default-only context with limitation |
| Profile metadata unreadable | yes | yes | context remains visible with limitation |
| Malformed Firefox `profiles.ini` | yes | yes | warning/limitation, no panic |
| Ambiguous selector | yes | yes | behavior follows selected ambiguity policy |
| No browsers/contexts detected | yes | yes | successful empty output with clear message |
| JSON schema drift | yes | yes | golden test fails before release |

Critical silent gaps after accepted decisions: 0.

### NOT in Scope

- `brw open <url> --context <selector>` launch behavior: deferred to PR2.
- Rules engine changes: deferred until discovery and context output are trustworthy.
- GUI/default-browser shim: long-term product surface, not part of PR1.
- crates.io publishing workflow: follow-up release task.

### Worktree Parallelization Strategy

| Step | Modules touched | Depends on |
|------|-----------------|------------|
| Context model + selector | `crates/browserware-types` | — |
| Profile fixtures + discovery | `crates/browserware-profiles` | context model |
| CLI contexts command/output | `crates/browserware-cli` | context model, profile discovery |
| Docs/test plan updates | `docs`, `.github/designs`, test fixtures | context model |

Parallel lanes:

- Lane A: context model + selector.
- Lane B: profile fixture design can start in parallel, but implementation waits for Lane A.
- Lane C: CLI command shell can start in parallel, but JSON shape waits for Lane A.
- Lane D: docs can run throughout.

Execution order: start Lane A first. Lane B and C can prepare fixtures/command shells while A lands. Merge A, then finish B + C in parallel worktrees. Lane D updates last.

Conflict flags: `browserware-types` is the shared contract, so avoid parallel edits there after Lane A starts.

### Review Completion Summary

- Step 0: Scope Challenge — scope reduced per recommendation.
- Architecture Review: 4 issues found, 4 resolved.
- Code Quality Review: 1 issue found, 1 resolved.
- Test Review: diagram produced, 2 gaps identified, 2 resolved as plan requirements.
- Performance Review: 0 issues found.
- NOT in scope: written.
- What already exists: written.
- TODOS.md updates: 1 item proposed, 1 accepted.
- Failure modes: 0 critical gaps flagged after accepted decisions.
- Outside voice: skipped.
- Parallelization: 4 lanes, 2 can proceed in parallel after the shared model lands.
- Lake Score: 7/7 recommendations chose complete option.

## GSTACK REVIEW REPORT

| Review | Trigger | Why | Runs | Status | Findings |
|--------|---------|-----|------|--------|----------|
| CEO Review | `/plan-ceo-review` | Scope & strategy | 0 | — | — |
| Codex Review | `/codex review` | Independent 2nd opinion | 0 | — | — |
| Eng Review | `/plan-eng-review` | Architecture & tests (required) | 1 | CLEAN | 7 issues, 0 critical gaps |
| Design Review | `/plan-design-review` | UI/UX gaps | 0 | — | — |
| DX Review | `/plan-devex-review` | Developer experience gaps | 0 | — | — |

**UNRESOLVED:** 0
**VERDICT:** ENG CLEARED — ready to implement PR1.

---

## /autoplan CEO Review — Phase 1

*Generated by /autoplan on 2026-04-11. Branch: pr1/browser-context-substrate. Commit: f8c512f.*

**Status note:** PR1 implementation is COMPLETE (36 commits ahead of main). This is a pre-ship strategy review, not a pre-implementation review.

### Pre-Review System Audit

**Recent history:** 36 commits ahead of main. The latest fix commits addressed review findings and CI failures — code already went through one review cycle.

**Diff stat:** 39 files changed, 3758 insertions(+), 1166 deletions(-).

**TODOs in codebase:** `browserware-detect/src/lib.rs`, `platform/windows.rs`, launch/rules/system scaffolds have deferred work items. All expected and in-scope deferred items.

**Critical gap found:** `README.md` says `cargo install browserware-cli` but TODOS.md confirms crates.io publishing is NOT set up. `AGENTS.md` marks crates.io as `✅` for all crates — aspirational, not actual. The install command will fail for anyone who tries it.

**Open PRs on this branch:** None. Branch not yet submitted as PR.

### Step 0A: Premise Challenge

| # | Premise | Status | Notes |
|---|---------|--------|-------|
| 1 | Developer CLI/library first, not consumer GUI | VALID | Consumer space saturated; substrate position is differentiated |
| 2 | Core object = BrowserContext (browser + profile + capabilities) | VALID | Implementation is clean; selector auto-computed from fields |
| 3 | First wow moment = `brw contexts --json` | MODERATE CHALLENGE | Discovery is "proof it works," not "saves me time." The actionable wow is `brw open --context work`. PR1 is the substrate, not the product yet. Framing matters for how PR1 is communicated. |
| 4 | Wedge user = developer with multiple browser contexts | VALID | Exactly the user who cares about --json and selectors |
| 5 | Rules sit on top of context discovery | VALID | Right architecture: garbage-in-garbage-out applies |
| 6 | GUI reuses same substrate | VALID | AGENTS.md confirms GUI is separate closed-source repo using library crates |

**Challenge to surface to user:** Is `brw contexts --json` a "wow moment" or a "foundation for the wow moment"? The PR1/PR2 sequencing is correct, but communicating PR1 as having a "wow" might set wrong expectations for early adopters who find it before `brw open --context` ships in PR2.

### Step 0B: Existing Code Leverage Map

| Sub-problem | Existing code | Status |
|-------------|--------------|--------|
| Browser detection (all 3 platforms) | `browserware-detect::detect_browsers()` | Done |
| Chrome profile discovery | `browserware-profiles::discover_chrome_profiles_from()` | Done |
| Firefox profile discovery | `browserware-profiles::discover_firefox_profiles_from()` | Done |
| Safari/unknown browser handling | `browserware-profiles::discover_profiles()` → `launch_only` | Done |
| BrowserContext model | `browserware-types::BrowserContext` | Done |
| Selector grammar + ambiguity | `browserware-types::ContextSelector`, `AmbiguityPolicy` | Done |
| CLI contexts command | `browserware-cli::commands::contexts` | Done |
| Table/JSON/plain output | `browserware-cli::output.rs` + inline in contexts.rs | Done |
| CLI integration tests | `browserware-cli/tests/cli.rs` | Done |
| Profile fixture tests | `browserware-profiles/tests/profiles.rs` | Done |

Everything PR1 requires is implemented. No parallel rebuilding of existing flows.

### Step 0C: Dream State Mapping

```
CURRENT STATE                  THIS PLAN (PR1)                  12-MONTH IDEAL
─────────────────────────────────────────────────────────────────────────────────
Browser detection works        BrowserContext model              brw open --context <selector>
 on macOS/Win/Linux             + profile discovery              rules engine: URL → context
                                + brw contexts --json/table      crates.io published and working
No public context API          + ContextSelector/Ambiguity       cargo install browserware-cli
                                + golden JSON tests              GUI shim using library crates
brw open is a stub             + fixture-driven profile tests   community scripts/agents use it
                                                                 Helium/ChatGPT Atlas browsing
README cargo install           PR1 ships the substrate.         automation use browserware API
fails (no crates.io)           PR2 ships the action.
```

Delta: PR1 moves from "detection exists" to "contexts are discoverable and scriptable." Right step.

### Step 0C-bis: Implementation Alternatives

The design doc already explored A/B/C approaches. Approach B was selected and implemented. Confirming selection:

| Approach | Status | Verdict |
|----------|--------|---------|
| A (Context CLI Wedge — ship contexts + open together) | Considered | Would have required browser-specific launch flags in PR1; scope explosion risk |
| B (Substrate First — model defines everything) | IMPLEMENTED | BrowserContext/selector infrastructure is clean; PR2 adds launch on top |
| C (Automation Gateway — agent/script-first APIs) | Considered | Correct long-term direction; compatible with Approach B foundation |

RECOMMENDATION: Approach B as implemented. No alternative would have been better.

### Step 0D: Mode — SELECTIVE EXPANSION (auto-decided: feature implementation)

Expansion candidates (auto-decided using 6 principles):

| Candidate | Decision | Principle | Rationale |
|-----------|----------|-----------|-----------|
| `--filter family=chromium` flag for `brw contexts` | Defer to PR2 | P3 (pragmatic) | Outside PR1 scope; deferred in plan |
| `--selector-only` flag to print just selectors | Defer | P5 (explicit) | Nice but not blocking |
| crates.io publishing workflow | MUST SHIP or fix docs | P1 (completeness) | README promises it; broken install is a DX disaster |
| Windows Chrome profile discovery gap | Verify before shipping | P1 (completeness) | In blast radius; TODO in windows.rs |

### Step 0E: Temporal Interrogation

```
HOUR 1 (setup/PR creation):    PR description needs to be clear about PR1 scope.
                                `brw open --context` is not in this PR — say so explicitly.
                                Link to PR2 plan.

HOUR 2-3 (reviewer questions): What browsers does CI have? Do golden tests use fixtures?
                                (Yes they do — profile tests use fixture dirs, not live discovery)
                                Does Windows CI pass Chrome profile discovery?

HOUR 4-5 (CI issues):          CI runs on ubuntu-latest, macos-latest, windows-latest.
                                Profile fixture tests use relative paths to fixtures/ dirs.
                                Should be platform-agnostic. Verify.

HOUR 6+ (pre-merge):           README cargo install accuracy.
                                CHANGELOG entry for 0.2.0 (already in CHANGELOG.md).
                                Stale PR#10 (detect-linux) should be checked.
```

### Step 0F: Mode = SELECTIVE EXPANSION (committed)

Implementation approach confirmed: Approach B (substrate first). PR1 scope as defined.


---

## /autoplan CEO Review — Phase 1 (Continued)

### Dual-Voice Consensus Table

> Codex voice unavailable (session expired). Claude CEO findings only.

| Finding | Severity | Consensus |
|---------|----------|-----------|
| `brw contexts --json` is infrastructure proof, not the user wedge | HIGH | CONFIRMED. PR1 is correct as "substrate milestone." Reframe in PR description. |
| No demand evidence for wedge user | HIGH | CONFIRMED. Not blocking PR1 (library, not SaaS), but matters for positioning. |
| Browsers closing API gaps (Arc→Dia encrypted profiles) | CRITICAL | CONFIRMED. Mitigation: ship fast, document limitations honestly, own the abstraction layer. |
| Approach C (MCP/automation gateway) dismissed too quickly | MEDIUM | CONFIRMED. Not for PR1. Flag as PR3 or separate issue. |
| Apple competitive risk; crates.io library play is the moat | HIGH | CONFIRMED. AGENTS.md already calls out library crates as primary value. |

**Decision: Phase 1 CLEAR. No blockers requiring user input beyond already-approved Premise 3 reframe.**

---

### CEO Section 1: Architecture Review

**Crate boundaries are correct.** The dependency graph matches the intended design: `types` at the bottom, `detect` and `profiles` above, `cli` at the top. No circular dependencies detected.

**`home_dir()` custom implementation** in `browserware-profiles/src/lib.rs:197`: reads `HOME` (Unix), falls back to `USERPROFILE` (Windows-only). For `chrome_user_data_dir` on Windows, the code correctly uses `LOCALAPPDATA` first, falling back to `home_dir().join("AppData/Local")`. This is correct — `LOCALAPPDATA` is the right Windows env var for Chrome user data. The fallback chain is sound.

**`BrowserFamily::Other` catch-all** in `discover_profiles` correctly returns `launch_only`. No dead paths.

**Selector grammar** is clean: percent-encode `,`, `=`, `%`. Encode/decode round-trip is tested. Canonical and alias formats both work. Auto-dispatch in `parse()` is correct.

**Verdict: CLEAN. Architecture matches the stated vision.**

---

### CEO Section 2: Error & Rescue Map

| Error Scenario | Handling | Verdict |
|----------------|----------|---------|
| Chrome `Local State` missing/unreadable | `launch_only` + `tracing::warn!` | GOOD |
| Chrome `Local State` is invalid JSON | `launch_only` (no logging) | MINOR: should log |
| Chrome `Local State` has no `profile.info_cache` | `launch_only` | GOOD |
| Firefox `profiles.ini` missing/unreadable | `launch_only` + `tracing::warn!` | GOOD |
| Firefox `profiles.ini` has no `[Profile*]` sections | `launch_only` ("contains no profiles") | GOOD |
| Profile section with no `Name=` line | Silently skipped (section flushed only when `current_name` is `Some`) | CORRECT |
| WebKit/Safari | `launch_only` always | CORRECT |
| Unknown browser family | `launch_only` | CORRECT |
| Ambiguous selector | `AmbiguityPolicy` (First/Warn/Error) | GOOD |

**One minor gap:** `chrome.rs:52` — `serde_json::from_str` failure is returned as `launch_only` but does not log via `tracing`. Inconsistent with the I/O error branch above it that does log. Non-blocking.

**Verdict: MOSTLY CLEAN. One missing `tracing::warn!` on invalid JSON branch — flagged as minor.**

---

### CEO Section 3: Security Review

- All reads are the user's own home directory files (`Local State`, `profiles.ini`). No privilege escalation.
- No shell command execution. No `std::process::Command` in profile discovery paths.
- Path construction uses `PathBuf::join` throughout, not string concatenation. No path traversal risk.
- JSON parsing via `serde_json` — no eval, no unsafe deserialization.
- `#![forbid(unsafe_code)]` declared in `browserware-profiles/src/lib.rs`.
- Selector parsing validates all segments — unknown keys return `Err`, not silently ignored.

**Verdict: CLEAN. No security concerns.**

---

### CEO Section 4: Data Flow Review

```
detect_browsers()
  └─ returns Vec<Browser> (id, name, executable, variant, family, version)

for each Browser:
  discover_profiles(&browser)
    └─ dispatches on browser.variant
       ├─ Chromium → chrome_user_data_dir() → discover_chrome_profiles_from()
       ├─ Firefox  → firefox_profiles_ini() → discover_firefox_profiles_from()
       ├─ WebKit   → launch_only()
       └─ Other    → launch_only()
    └─ returns ProfileDiscovery { profiles: Vec<ProfileRef>, capability: LaunchCapability }

BrowserContext::new(browser, profile, capability)
  └─ selector auto-computed via build_selector()
  └─ returns BrowserContext { browser, profile, selector, capability }

Vec<BrowserContext>.sort_by(selector)
  └─ stable, deterministic ordering for JSON output
```

**Selector stability:** selectors are deterministic given the same inputs. Chrome `Default` always sorts first (hardcoded in `chrome.rs` sort). Firefox profiles return in `.ini` file order. This means `brw contexts --json` output is **deterministically ordered** — good for scripting.

**One observation:** if Chrome `Local State` key order changes (it's a JSON object), profile order could change. The sort in `discover_chrome_profiles_from` fixes this: `Default` first, then alphabetical by `id`. Correct.

**Verdict: CLEAN. Data flow is explicit, deterministic, and well-ordered.**

---

### CEO Section 5: Code Quality

**Positives:**
- `#[must_use]` on all public API methods that return values.
- `const fn launch_label()` in CLI contexts command — correct use of `const`.
- Firefox parser: zero dependencies, single-pass, handles `let Some(name)` with `let-else` syntax correctly.
- Chrome parser: `meta["name"].as_str().unwrap_or(dir_name)` — correct graceful fallback when `name` field is missing.
- `ContextSelector::select` uses `iter().filter()` without collecting — no unnecessary allocation on the fast path.

**Minor issues:**
1. `chrome.rs:52` — missing `tracing::warn!` on JSON parse failure (noted above).
2. `browserware-profiles/src/lib.rs` `home_dir()` function — custom-rolled. Not wrong, but note it for future maintenance. If the `home` crate is ever added as a dep (likely), this can be removed.
3. Firefox parser: `ProfileRef { id: name.clone(), display_name: name }` — profile `id` equals `display_name` for Firefox. This is correct per the design (Firefox profile names are human-readable), but could be misleading to future contributors expecting `id` to be an opaque key like Chrome's `"Profile 1"`.

**Verdict: HIGH QUALITY. Minor polish items, not blockers.**

---

### CEO Section 6: Test Review

| Test Area | Count | Coverage Assessment |
|-----------|-------|---------------------|
| `browserware-types::context` | 8 tests | Round-trips, selector, encoding — FULL |
| `browserware-types::selector` | 22 tests | Parse, match, select, ambiguity policies — FULL |
| `browserware-profiles::chrome` | 4 tests | Multi, single, missing, malformed — FULL |
| `browserware-profiles::firefox` | 5 tests | Multi, default, missing, empty, malformed section — FULL |
| `browserware-profiles::lib` | 2 tests | WebKit + Other dispatch — FULL |
| `browserware-cli::contexts` | 9 tests | Headers, count, selector, empty, JSON, plain — FULL |
| CLI integration | ~12 tests | `brw contexts` subcommand, JSON, plain, global format flag — GOOD |

**Fixture-driven profile tests** use `tests/fixtures/` directories with real-format data. Correct approach for CI portability.

**What's missing:**
- No test for Chrome profile with `name` field absent (falls back to dir name). The code handles it, but no explicit test.
- No test for `ContextSelector::Warn` variant actually emitting a tracing warning (hard to test, acceptable to skip).

**Verdict: STRONG TEST COVERAGE. 44 tests passing, two minor gaps noted.**

---

### CEO Section 7: Performance

- `discover_profiles()` is synchronous + filesystem-based. For a CLI tool invoking this N times (N = number of installed browsers), typical N is 2–5. Each invocation does 1 `fs::read_to_string`. Total: 2–5 small file reads per `brw contexts` run. No performance concern.
- No caching (intentional — `brw` is a short-lived process).
- `ContextSelector::select` is O(n) in contexts — no indexing. For typical machine (< 20 contexts), this is fine.
- `sort_by(selector)` is string comparison on short strings. Fine.

**Verdict: CLEAN. No performance concerns for the CLI use case.**

---

### CEO Section 8: Observability

- `tracing::warn!` on file read failures — correct instrument.
- No `tracing::info!` or `tracing::debug!` on happy path — CLI tools don't need this by default.
- JSON output has stable field names — scriptable, observable from the outside.
- `limitations` array in `LaunchCapability` provides machine-readable capability explanations — excellent for tooling consumers.

**Verdict: ADEQUATE for a CLI tool. No gaps.**

---

### CEO Section 9: Deployment Readiness

| Check | Status |
|-------|--------|
| `cargo test --workspace` | ✅ 44 passed, 0 failed |
| Clippy pedantic + nursery | Assumed clean (CI enforced) |
| `cargo fmt` | Assumed clean (CI enforced) |
| CHANGELOG.md entry for 0.2.0 | ✅ Present, complete |
| CI matrix (macOS/Linux/Windows) | ✅ Configured per AGENTS.md |
| Profile fixture tests (platform-agnostic paths) | ✅ Uses `CARGO_MANIFEST_DIR` |
| crates.io publishing workflow | ❌ NOT SET UP — README lies |
| PR description written | ❌ Not yet created |

**CRITICAL GAP — must resolve before shipping PR1:**
`README.md` says `cargo install browserware-cli`. `TODOS.md` says crates.io publishing is not set up. This will create broken install instructions the moment someone finds the repo. Either remove the install command from README or add a note that it's not yet published.

**Verdict: NEEDS ACTION on README before merge.**

---

### CEO Section 10: Long-Term Trajectory

**Moat analysis:**
The real moat is `browserware-types` as a shared contract layer. If `brw open --context <selector>` works reliably, and if the `ContextSelector` grammar becomes a stable interface that agents/scripts/GUIs depend on, then Browserware owns the abstraction layer regardless of what browser vendors do.

**Competitive risks:**
1. **Arc→Dia encrypted profiles** — already documented in Velja's public limitations. Browserware should document this explicitly in `limitations` vector output, not hide it.
2. **Apple profile routing** — low probability, but if macOS 16+ ships native "open in Safari Work profile," that's a threat to the GUI play. Library crates survive this.
3. **Chrome Profile API** — if Google ships a public profile launch API, Chrome support improves for everyone. This is actually *good* for Browserware (removes the `Local State` parsing dependency).

**Opportunities:**
1. **MCP tool** — `browserware_contexts()` and `browserware_open(url, selector)` as an MCP server would make this available to every Cursor/Claude Code/Windsurf user. High distribution leverage. PR3 candidate.
2. **Helium browser** — already registered in `detect` crate. First mover in the AI browser routing space.
3. **crates.io publishing** — the library crates are the right play. Get them published.

**Verdict: TRAJECTORY IS CORRECT. Three tactical items to track (README fix, MCP opportunity, crates.io publishing).**

---

### CEO Phase 1 Completion Summary

**Overall verdict: PR1 is READY TO SHIP after one fix and one documentation clarification.**

| Issue | Severity | Action |
|-------|----------|--------|
| README `cargo install` lies (crates.io not set up) | HIGH | Fix before merge: remove or annotate |
| Chrome malformed JSON branch missing `tracing::warn!` | LOW | Fix in PR1 or create follow-up issue |
| PR1 should be framed as "substrate milestone" not "wow moment" | FRAMING | Address in PR description |
| Windows Chrome profile discovery gap (noted in `windows.rs`) | MEDIUM | Verify in CI or document |
| MCP tool opportunity (Approach C) | OPPORTUNITY | Track as future issue |

**Premise 3 reframe (user-approved):** PR1 is the substrate proof. The wow moment is `brw open --context work` in PR2. PR description should say this explicitly.

**Tests: 44 passing. Architecture: clean. Security: clean. Data flow: deterministic.**

**Phase 1 (CEO Review): COMPLETE. Proceeding to Phase 3 (Eng Review). Phase 2 skipped (no UI scope).**


---

## /autoplan Phase 3: Engineering Review

> **Context note:** PR1 was already merged to main (commit fa162db) before this review completed. These findings become action items for a follow-up PR.

### CRITICAL (Bugs in shipped code)

**1. Linux: `chrome-beta` and `chrome-dev` map to stable Chrome path**
`crates/browserware-profiles/src/lib.rs:145`
```rust
"chrome" | "chrome-beta" | "chrome-dev" => "google-chrome",
```
Should be:
- `chrome-beta` → `google-chrome-beta`
- `chrome-dev` → `google-chrome-unstable`

Users running Chrome Beta on Linux will see Chrome Stable's profiles instead. Wrong profiles shown and wrong profile targeted at launch.

**2. Linux: `librewolf`, `waterfox`, `floorp` use `~/.mozilla/` base — wrong**
`crates/browserware-profiles/src/lib.rs:211–213`
Linux block: `base = ~/.mozilla`, then appends `librewolf/profiles.ini` etc.
Actual paths: `~/.librewolf/profiles.ini`, `~/.waterfox/profiles.ini`, `~/.floorp/profiles.ini`.
Discovery silently fails for all three on Linux.

**3. Windows: LibreWolf has doubled directory component**
`crates/browserware-profiles/src/lib.rs:228`
```rust
"librewolf" => "librewolf/librewolf/profiles.ini",
```
Resolves to `%APPDATA%\librewolf\librewolf\profiles.ini`. Correct path: `%APPDATA%\librewolf\profiles.ini`.

### HIGH (Fix soon)

**4. `BrowserContext` deserialize does not recompute selector — invariant can break**
`crates/browserware-types/src/context.rs:65–101`
`selector` is private and computed at construction. `#[derive(Deserialize)]` reads it directly from JSON. A `BrowserContext` deserialized from edited JSON carries a stale selector. Add `#[serde(skip)]` on `selector` and implement custom `Deserialize` that recomputes it from `browser`+`profile`.

**5. Firefox INI: empty `Name=` produces ProfileRef with `id = ""`**
`crates/browserware-profiles/src/firefox.rs:62–63`
`Name=` (empty) or `Name=   ` produces `current_name = Some("")`. Flush pushes a `ProfileRef` with empty id and display_name. Add empty check before storing `current_name`.

**6. Windows: `floorp` missing from Firefox paths**
`crates/browserware-profiles/src/lib.rs:219–232`
macOS and Linux include `floorp`; Windows block does not. Actual path: `%APPDATA%\Floorp\profiles.ini`.

### MEDIUM

**7. `parse_alias` silently accepts `"chrome:work:extra"` — profile becomes `"work:extra"`**
`crates/browserware-types/src/selector.rs:131` — `split_once(':')` stops at first colon. Undocumented behavior, untested.

**8. `format_json` missing trailing newline** (table and plain both end with `\n`)
`crates/browserware-cli/src/commands/contexts.rs:180`

**9. `launch_label` `"limited"` branch unreachable** — no public constructor creates `launchable = false`
`crates/browserware-cli/src/commands/contexts.rs:190–197`

**10. Linux: `edge-canary` missing** from Chrome paths table
`crates/browserware-profiles/src/lib.rs:142–156`

### LOW / POLISH

**11. `home_dir()` on Windows: `HOME` takes priority over `USERPROFILE`** — swap order, Windows canonical var is `USERPROFILE`
`crates/browserware-profiles/src/lib.rs:245–256`

**12. `#[must_use]` on `Result`-returning methods is redundant** (Result is already `#[must_use]`)
`crates/browserware-types/src/selector.rs:84,126,156`

### CLEAN (Explicitly correct)

- CRLF handling in Firefox INI parser: `str::lines()` + `trim()` handles `\r\n` correctly.
- Chrome `info_cache` sort: explicit sort makes output deterministic regardless of JSON map order.
- `decode_selector_value` boundary check is correct (`index + 2 >= bytes.len()`).
- Round-trip encode/decode for `%`, `,`, `=` — tested and correct.
- `select()` ambiguity count (`2 + iter.count()`) is correct.
- No `unwrap()` / `expect()` in production paths.
- macOS and Windows Chrome/Firefox directory paths are correct.
- `discover_contexts()` sort by selector produces stable, deterministic ordering.
- All 52 tests pass. Clippy clean.

### Phase 3 Verdict

**3 critical bugs in shipped Linux/Windows profile path mappings. Fix in PR2 or dedicated patch PR.**
Architecture, test coverage, and core abstractions are solid. No structural changes needed.


---

## /autoplan Phase 3.5: DX Review

> Reviewing developer experience: CLI UX, time-to-hello-world, error messages, install path, and library ergonomics.

### CRITICAL: `cargo install browserware-cli` fails — crates.io not published

README.md says `cargo install browserware-cli`. TODOS.md confirms crates.io publishing is not set up. Anyone who finds this repo and follows the README gets a hard failure on the first command. This is the worst possible first-impression bug.

**Action:** Either add `> Note: not yet published to crates.io — install from source:` + `cargo install --git ...`, or set up the crates.io publishing workflow (TODOS.md item).

### HIGH: AGENTS.md claims `crates.io ✅` for all crates — aspirational, not actual

`AGENTS.md` architecture table marks all 7 library crates as published. Contributes to the README's false promise. Should be updated to reflect actual state.

### MEDIUM: `brw contexts` table output has no indication of which browsers couldn't be discovered

If Chrome profile directory is inaccessible, `brw contexts` shows the browser with `launch` capability label but no visible indication of *why* profiles couldn't be loaded. The `limitations` array exists in JSON output but is not surfaced in table format. Power users debugging "why don't I see my profiles?" have no CLI feedback.

**Suggestion:** Add a `⚠` or `[!]` marker to the LAUNCH column when `capability.limitations` is non-empty, or add a `--verbose` flag that shows limitations inline.

### MEDIUM: `brw open` is a stub — context discovery has no entry point for the user wow moment

`brw contexts --json` works. But the workflow users actually want — "open this URL in my work Chrome" — requires PR2. The CLI has no `brw open --context` command. A developer finding the repo today sees a useful list command but cannot do anything with it from the CLI.

**Suggestion:** PR description for PR1 should explicitly say "PR2 adds `brw open --context <selector>`" and link the plan. Set expectations correctly.

### LOW: `brw contexts` plain format sorts selectors but table format header alignment is fixed-width

If someone has a very long profile name (> 20 chars), the table column expands correctly (dynamic width calculation), but the minimum `profile_width = 7` means 1-char profile names get 6 spaces of padding. Not a bug, but worth noting the table auto-sizes correctly.

### LOW: `--format` flag accepts `table`, `json`, `plain` — but `--help` does not list valid values

`brw contexts --help` shows `--format <FORMAT>` but does not enumerate the accepted values. Users have to guess or read the source. Clap's `value_parser` with enum should emit the valid options in help text. Minor polish.

### CLEAN

- `brw contexts --format json` output is stable and scriptable. Correct choice to include `count` field.
- `brw browsers` and `brw contexts` both work on macOS live system. Commands return consistent structure.
- `AmbiguityPolicy` in the library crate is the right abstraction for downstream callers.
- `limitations` array in JSON output is excellent for tooling consumers that need to understand capability gaps.
- Error messages from `ContextSelector::parse` are specific: names the bad segment, names valid keys. Good.

### Phase 3.5 Verdict

**One critical DX bug (broken install instructions in README). One important messaging gap (PR2 not yet shipped).** Library API is ergonomic. CLI UX is clean. Fix README before the repo gets wider distribution.


---

## /autoplan Phase 4: Final Gate

> **Status:** PR1 (commit fa162db) is already merged. This review is a post-merge retrospective and action item generator.

### Action Items — Ranked by Impact

| # | Item | Severity | File | Action |
|---|------|----------|------|--------|
| 1 | `cargo install browserware-cli` in README fails — not published | CRITICAL-DX | `README.md` | Remove or annotate with "not yet on crates.io" |
| 2 | Linux Chrome Beta/Dev map to stable Chrome path | CRITICAL-BUG | `browserware-profiles/src/lib.rs:145` | Fix path mappings for `chrome-beta` → `google-chrome-beta`, `chrome-dev` → `google-chrome-unstable` |
| 3 | Linux LibreWolf/Waterfox/Floorp use `~/.mozilla/` base — wrong | CRITICAL-BUG | `browserware-profiles/src/lib.rs:211` | Fix base to `~/`; correct paths are `~/.librewolf/`, `~/.waterfox/`, `~/.floorp/` |
| 4 | Windows LibreWolf doubled directory component | CRITICAL-BUG | `browserware-profiles/src/lib.rs:228` | Remove extra `librewolf/` from path |
| 5 | `BrowserContext` deserialize doesn't recompute selector | HIGH | `browserware-types/src/context.rs` | Add `#[serde(skip)]` + custom deserialize |
| 6 | Firefox `Name=` empty value produces `ProfileRef { id: "" }` | HIGH | `browserware-profiles/src/firefox.rs:62` | Skip empty `Name=` lines |
| 7 | Windows Floorp missing from Firefox paths | HIGH | `browserware-profiles/src/lib.rs:228` | Add `"floorp" => "Floorp/profiles.ini"` |
| 8 | AGENTS.md marks crates.io as ✅ — aspirational | HIGH-DX | `AGENTS.md` | Update to reflect actual publish status |
| 9 | Chrome malformed JSON branch missing `tracing::warn!` | MEDIUM | `browserware-profiles/src/chrome.rs:52` | Add `tracing::warn!` consistent with I/O error branch |
| 10 | `format_json` missing trailing newline | MEDIUM | `browserware-cli/src/commands/contexts.rs:180` | Add `\n` |
| 11 | `launch_label` `"limited"` branch unreachable | LOW | `browserware-cli/src/commands/contexts.rs:197` | Remove or add `LaunchCapability::discovery_only()` constructor |
| 12 | `home_dir()` on Windows: `HOME` before `USERPROFILE` | LOW | `browserware-profiles/src/lib.rs:245` | Swap priority on Windows |

### Taste Decisions (Auto-decided, no user input needed)

| Decision | Choice | Principle |
|----------|--------|-----------|
| Fix bugs in separate patch PR vs fold into PR2 | Patch PR (fix now) | P1 (completeness) — bugs are in shipped code |
| Fix README before wider distribution | Yes | P1 (completeness) — broken first impression |
| MCP tool (Approach C) | Track as future issue | P3 (pragmatic) — not blocking |
| crates.io publishing | TODOS.md item, must resolve before public announcement | P1 (completeness) |

### Final Assessment

**PR1 shipped a clean substrate.** The `BrowserContext` model, `ContextSelector` grammar, fixture-driven tests, and CLI output format are well-designed. The architecture is correct.

**What got shipped with bugs:** Profile path mappings for non-primary browsers on Linux (Chrome Beta/Dev, LibreWolf, Waterfox, Floorp) and Windows (LibreWolf doubled path). These are real-world bugs that will affect users running non-Chrome-Stable browsers.

**Recommended next steps:**
1. Fix README `cargo install` instruction (30-min task)
2. Create patch PR for profile path bugs (items 2–4 above) before PR2 lands
3. Fix Firefox empty `Name=` bug and `BrowserContext` deserialize invariant in same patch
4. Update AGENTS.md crates.io status
5. Track MCP tool as a separate issue

**Architecture rating: 8/10. Shipped code correctness: 7/10 (path bug cluster drops it). Test strategy: 9/10.**

---

**/autoplan complete. Review written to this file. Action items above are the PR2 / patch PR prep list.**

